#echo 'ag -l '`implode ' |  xargs -d "\n" ag -l ' $@ ` ' | xargs -d "\n" grep ^
eval  'grep -ril '`implode ' |  xargs -d "\n" grep -ril ' $@ `' | xargs -d "\n" grep ^ | ag '` implode ' | ag ' $@`' | strlim '
