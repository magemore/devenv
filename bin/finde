#echo 'ag -l '`implode ' |  xargs -d "\n" ag -l ' $@ ` ' | xargs -d "\n" grep ^
eval  'ag -l '`implode ' |  xargs -d "\n" ag -l ' $@ `' | xargs -d "\n" grep ^ | ag '` implode ' | ag ' $@`' | strlim '
